steps:
- extract_molecules_sdf:
    in:
      input_path: ../data/NCIOpen.sdf
      output_sdf_path: '&ligand.sdf' # singular
      first_molecule: "1"
      last_molecule: "1"
- setup_receptor.yml:
- dsb.yml:

wic:
  steps:
    (3, dsb.yml):
      in:
        sdf_path: '*ligand.sdf'